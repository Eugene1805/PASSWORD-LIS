<Window x:Class="PASSWORD_LIS_Client.Views.FriendRequestsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PASSWORD_LIS_Client.Views"
        mc:Ignorable="d"
        Title="FriendRequestsWindow" Height="400" Width="300">
    <Grid>
        <ListView ItemsSource="{Binding PendingRequests}" SelectedItem="{Binding SelectedRequest}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Nickname}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom" HorizontalAlignment="Center">
            <Button Content="Aceptar" Command="{Binding AcceptRequestCommand}"/>
            <Button Content="Rechazar" Command="{Binding RejectRequestCommand}"/>
        </StackPanel>
    </Grid>
</Window>
