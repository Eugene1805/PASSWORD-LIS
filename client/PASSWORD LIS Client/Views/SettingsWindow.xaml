<Window x:Class="PASSWORD_LIS_Client.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PASSWORD_LIS_Client.Views"
        xmlns:language="clr-namespace:PASSWORD_LIS_Client.Properties.Langs"
        mc:Ignorable="d"
        Title="SettingsWindow" Height="250" Width="450">
    <Grid>
        <Border Style="{StaticResource floatingBorderStyle}" Padding="10" Margin="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="{x:Static language:Lang.settingsText}"
                        Style="{StaticResource titleTextBlockStyle}"
                        Margin="20,0,20,10"/>

                <Grid Grid.Row="1" Margin="20,0,20,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="{x:Static language:Lang.musicText}"
                            Style="{StaticResource subtitleTextBlockStyle}"
                            TextAlignment="Left"/>
                    <Slider Grid.Column="1" x:Name="musicLevelSlider" VerticalAlignment="Center"/>
                </Grid>

                <Grid Grid.Row="2" Margin="20,0,20,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="{x:Static language:Lang.soundEffectsText}"
                          Style="{StaticResource subtitleTextBlockStyle}"
                          TextAlignment="Left"/>
                    <Slider Grid.Column="1" x:Name="soundEffectsLevelSlider" VerticalAlignment="Center"/>
                </Grid>

                <Button Grid.Row="4" Content="{x:Static language:Lang.logOutText}"
                     Style="{StaticResource logOutButtonStyle}"
                     Width="180" Height="35"/>
            </Grid>
        </Border>
    </Grid>
</Window>
