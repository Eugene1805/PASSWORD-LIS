<Window x:Class="PASSWORD_LIS_Client.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PASSWORD_LIS_Client.Views"
        xmlns:language="clr-namespace:PASSWORD_LIS_Client.Properties.Langs"
        xmlns:viewModels="clr-namespace:PASSWORD_LIS_Client.ViewModels"
        xmlns:utils="clr-namespace:PASSWORD_LIS_Client.Utils"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        MinHeight="400" MinWidth="400" 
        Title="{utils:Translate logInText}" Height="600" Width="600">

    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="/Resources/BackgroudImage.png"/>
        </Grid.Background>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="2" Margin="10" VerticalAlignment="Center">

            <TextBlock Text="{utils:Translate logInText}" 
                   Style="{StaticResource titleBlackTextBlockStyle}" 
                   HorizontalAlignment="Center" 
                   Margin="0,0,0,20"
                   AutomationProperties.AutomationId="loginText"/>

            <TextBlock Text="{utils:Translate emailText}" Style="{StaticResource subtitleBlackTextBlockStyle}"
                       HorizontalAlignment="Left"/>
            <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"
                     AutomationProperties.AutomationId="emailTextBox"
                     Style="{StaticResource primaryTextBoxStyle}" Width="230"/>
            <TextBlock Text="{Binding EmailError}" Style="{StaticResource errorTextBlockStyle}" Margin="5,0,0,10"/>

            <TextBlock Text="{utils:Translate userPasswordText}" Style="{StaticResource subtitleBlackTextBlockStyle}"
                       HorizontalAlignment="Left"/>
            <PasswordBox Style="{StaticResource passwordboxStyle}" Width="230"
                         utils:PasswordBoxHelper.BindPassword="True" AutomationProperties.AutomationId="passwordBox"
                         utils:PasswordBoxHelper.BoundPassword="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="{Binding PasswordError}" Style="{StaticResource errorTextBlockStyle}" Margin="5,0,0,10"/>

            <TextBlock Style="{StaticResource bodyTextBlockStyle}" TextWrapping="Wrap" Margin="0,10,0,0" HorizontalAlignment="Right">
            <Hyperlink Command="{Binding NavigateToForgotPasswordCommand}"> 
                <Run Text="{utils:Translate forgotUserPasswordText}"/>
            </Hyperlink>
            </TextBlock>

            <Button Content="{utils:Translate logInText}" 
                Style="{StaticResource LoadingButtonStyle}"
                utils:ButtonProperties.IsBusy="{Binding IsLoggingIn}"
                AutomationProperties.AutomationId="loginButton"
                Width="210" 
                Height="40" 
                Margin="0,15,0,10" 
                HorizontalAlignment="Center"
                Command="{Binding LoginCommand}"
                />

            <Button Content="{utils:Translate playAsGuestText}" 
                Style="{StaticResource logOutButtonStyle}" 
                Width="210" 
                Height="40" 
                Margin="0,15,0,10" 
                HorizontalAlignment="Center"
                Command="{Binding PlayAsGuestCommand}"
                AutomationProperties.AutomationId="playAsGuestButton"/>

            <TextBlock Style="{StaticResource bodyTextBlockStyle}" TextWrapping="Wrap" HorizontalAlignment="Center">
            <Run Text="{utils:Translate noAccountText}"/>
            <Hyperlink Command="{Binding NavigateToSignUpCommand}"> 
                <Run Text="{utils:Translate signUpText}"/>
            </Hyperlink>
            </TextBlock>
        </StackPanel>
    </Grid>
</Window>
