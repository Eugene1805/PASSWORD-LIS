<Window x:Class="PASSWORD_LIS_Client.Views.SignUpWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PASSWORD_LIS_Client.Views"        
        xmlns:viewModels="clr-namespace:PASSWORD_LIS_Client.ViewModels"
        xmlns:utils="clr-namespace:PASSWORD_LIS_Client.Utils"
        xmlns:language="clr-namespace:PASSWORD_LIS_Client.Properties.Langs"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        MinHeight="700" MinWidth="700"
        Title="{utils:Translate signUpText}" Height="700" Width="700">
   
    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="/Resources/BackgroudImage.png"/>
        </Grid.Background>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="2" Margin="20">

            <TextBlock Text="{utils:Translate createAccountText}" 
               Style="{StaticResource titleBlackTextBlockStyle}" 
               HorizontalAlignment="Center" 
               Margin="0,0,0,20"/>

            <TextBlock Text="{utils:Translate nameText}" Style="{StaticResource subtitleBlackTextBlockStyle}" HorizontalAlignment="Left"/>
            <TextBox Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}" MaxLength="50"
                     x:Name="firstNameTextBox" Style="{StaticResource primaryTextBoxStyle}" Width="300"/>

            <TextBlock Text="{utils:Translate lastNameText}" Style="{StaticResource subtitleBlackTextBlockStyle}" HorizontalAlignment="Left"/>
            <TextBox Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}" MaxLength="80"
                     x:Name="lastNameTextBox" Style="{StaticResource primaryTextBoxStyle}" Width="300"/>

            <TextBlock Text="{utils:Translate nicknameText}" Style="{StaticResource subtitleBlackTextBlockStyle}" HorizontalAlignment="Left"/>
            <TextBox Text="{Binding Nickname, UpdateSourceTrigger=PropertyChanged}" MaxLength="50"
                     x:Name="nicknameTextBox" Style="{StaticResource primaryTextBoxStyle}" Width="300"/>

            <TextBlock Text="{utils:Translate emailText}" Style="{StaticResource subtitleBlackTextBlockStyle}" HorizontalAlignment="Left"/>
            <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" MaxLength="100"
                     x:Name="emailTextBox" Style="{StaticResource primaryTextBoxStyle}" Width="300"/>

            <TextBlock Text="{utils:Translate userPasswordText}" Style="{StaticResource subtitleBlackTextBlockStyle}" HorizontalAlignment="Left"/>
            <PasswordBox x:Name="passwordBox" Style="{StaticResource passwordboxStyle}" Width="300" MaxLength="15"
                         utils:PasswordBoxHelper.BindPassword="True"
                        utils:PasswordBoxHelper.BoundPassword="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="{utils:Translate confirmUserPasswordText}" Style="{StaticResource subtitleBlackTextBlockStyle}" HorizontalAlignment="Left"/>
            <PasswordBox x:Name="confirmPasswordBox" Style="{StaticResource passwordboxStyle}" Width="300" MaxLength="15"
                         utils:PasswordBoxHelper.BindPassword="True"
                         utils:PasswordBoxHelper.BoundPassword="{Binding ConfirmPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Style="{StaticResource bodyTextBlockStyle}" TextWrapping="Wrap" Margin="0,10,0,0">
        <Hyperlink Command="{Binding OpenTermsAndConditionsCommand}"> 
            <Run Text="{utils:Translate termsAndConditionsText}"/>
        </Hyperlink>
            </TextBlock>

            <Button x:Name="signUpButton"
                Content="{utils:Translate signUpText}" 
                Style="{StaticResource LoadingButtonStyle}"
                utils:ButtonProperties.IsBusy="{Binding IsSigningUp}"
                Width="250" 
                Height="35" 
                Margin="0,15,0,10" 
                HorizontalAlignment="Center"
                Command="{Binding SignUpCommand}"
                />

            <TextBlock TextWrapping="Wrap" 
              Style="{StaticResource bodyTextBlockStyle}">
                <Run Text="{utils:Translate alreadyHaveAccountText}"/>
                <Separator Margin="10,10,30,10"/>
                <Hyperlink Command="{Binding NavigateToLoginCommand}"> 
                    <Run Text="{utils:Translate logInText}"/>
                </Hyperlink>
                <LineBreak/>
                <Separator Margin="10"/>
                <Run Text="{utils:Translate codeActivationMessageText}"/>
            </TextBlock>
        </StackPanel>
    </Grid>
    
</Window>
