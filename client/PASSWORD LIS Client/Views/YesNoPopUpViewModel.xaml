<Window x:Class="PASSWORD_LIS_Client.Views.YesNoPopUpViewModel"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:language="clr-namespace:PASSWORD_LIS_Client.Properties.Langs"
        xmlns:local="clr-namespace:PASSWORD_LIS_Client.Views"
        WindowStartupLocation="CenterScreen"
        WindowStyle="ToolWindow"        
        mc:Ignorable="d"
        Title="{Binding Title}" Height="200" Width="400">
    <!-- QUEDA PENDIENTE HASTA ARREGLAR EL METODO DE ACTUALIZAR EL PROFILE Y CAMBIARLO
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="{Binding Message}"
                   TextWrapping="Wrap"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   FontSize="14"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,0">
            <Button Content="{Binding Source={StaticResource LangProvider}, Path=[yesButtonText]}"
                    Command="{Binding YesCommand}"
                    Style="{StaticResource primaryButtonStyle}"  
                    Width="100" Margin="10,0"/>
            <Button Content="{Binding Source={StaticResource LangProvider}, Path=[noButtonText]}"
                    Command="{Binding NoCommand}"
                    Style="{StaticResource logOutButtonStyle}" Width="100" Margin="10,0"/>
        </StackPanel>
    </Grid>
    -->
    <!--
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Image x:Name="iconImage" Width="50" Height="50" 
                   Grid.Column="0"
                   VerticalAlignment="Center" 
                   HorizontalAlignment="Center"
                   Margin="0,0,15,0"/>

            <ScrollViewer Grid.Column="1" 
                         VerticalAlignment="Center"
                         HorizontalAlignment="Stretch"
                         MaxHeight="120"
                         VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="messageTextBlock" 
                           Style="{StaticResource subtitleBlackTextBlockStyle}" 
                           Text="This is a pop-up message!" 
                           TextWrapping="Wrap"/>
            </ScrollViewer>
        </Grid>
    </Grid>
    -->
</Window>
