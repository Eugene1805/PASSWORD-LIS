<Window x:Class="PASSWORD_LIS_Client.Views.PopUpWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:language="clr-namespace:PASSWORD_LIS_Client.Properties.Langs"
        xmlns:utils="clr-namespace:PASSWORD_LIS_Client.Utils"
        WindowStartupLocation="CenterScreen"
        WindowStyle="ToolWindow"
        mc:Ignorable="d"
        Title="PopUpWindow" Height="200" Width="320">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Image x:Name="iconImage" Width="50" Height="50" 
                   Grid.Column="0"
                   VerticalAlignment="Center" 
                   HorizontalAlignment="Center"
                   Margin="0,0,15,0"/>

            <ScrollViewer Grid.Column="1" 
                         VerticalAlignment="Center"
                         HorizontalAlignment="Stretch"
                         MaxHeight="120"
                         VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="messageTextBlock" 
                           Style="{StaticResource subtitleBlackTextBlockStyle}" 
                           Text="This is a pop-up message!" 
                           TextWrapping="Wrap"/>
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>
