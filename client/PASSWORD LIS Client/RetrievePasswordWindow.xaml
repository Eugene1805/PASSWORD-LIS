<Window x:Class="PASSWORD_LIS_Client.RetrievePasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PASSWORD_LIS_Client"
        xmlns:language="clr-namespace:PASSWORD_LIS_Client.Properties.Langs"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        MinHeight="350" MinWidth="400"
        Title="{x:Static language:Lang.retrieveUserPasswordText}" Height="350" Width="400">
    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="Resources/BackgroudImage.png"/>
        </Grid.Background>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="20">
                <TextBlock Text="{x:Static language:Lang.retrieveUserPasswordText}" 
                   FontSize="20"  
                   TextWrapping="Wrap"
                   HorizontalAlignment="Center" 
                   Margin="0,0,0,20"/>
                <Label Content="{x:Static language:Lang.emailText}"/>
                <TextBox x:Name="emailTextBox" Width="160" Margin="0,10,10,0"/>
                <Button Content="{x:Static language:Lang.sendCodeText}" 
                FontWeight="Bold" 
                Width="160" 
                Height="35" 
                Margin="0,15,0,10" 
                HorizontalAlignment="Center"
                Click="ButtonClickSendCode"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
